return {
    'feline-nvim/feline.nvim',
    -- event = "VeryLazy",
    init = function()
        vim.opt.termguicolors = true
    end,
    config = function()
        require('feline').setup()
        -- require('feline').winbar.setup()
    end,
    opts = {
    },
}

